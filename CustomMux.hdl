CHIP CustomMux {
    IN a, b, sel;
    OUT out;

    PARTS:
    CustomNot(in=sel, out=notsel);
    CustomAnd(a=a, b=notsel, out=anotsel);
    CustomAnd(a=b, b=sel, out=bsel);
    CustomOr(a=anotsel, b=bsel, out=out);
}